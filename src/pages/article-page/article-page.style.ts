import { styled } from "@mui/system";

type ArticlePageStylesType = object;

export const ArticlePageStyle = styled('div')<ArticlePageStylesType>`
  background-color: #FFFFFF;
  padding: 15px;
  filter: drop-shadow(0px 4px 12px rgba(0, 0, 0, 0.15));
  border-radius: 5px;
`;
