import { styled } from "@mui/system";

type PostStylesType = object;

export const PostStyle = styled('div')<PostStylesType>`
background: #FFFFFF;
border-radius: 5px;
filter: drop-shadow(0px 4px 12px rgba(0, 0, 0, 0.15));
padding: 15px;
width: 908px;
height: 82px;
`;
