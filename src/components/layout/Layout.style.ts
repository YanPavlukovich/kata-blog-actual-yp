import { styled } from '@mui/system';

type LayoutStylesType = object;

export const LayoutStyle = styled('div')<LayoutStylesType>`
  width: 938px;
  margin: 0 auto;
  padding-top: 26px;
  min-height: 810px;
`;
