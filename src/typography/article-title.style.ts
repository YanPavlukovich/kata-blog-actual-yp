import { styled } from "@mui/system";

type ArticleTitleStylesType = object;

export const ArticleTitleStyle = styled('div')<ArticleTitleStylesType>`
  .title {
		font-style: normal;
		font-weight: normal;
		line-height: 28px;
		color: #1890FF;
		margin: 0;
	}

`;
